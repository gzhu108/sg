#include "QuicStreamId.h"

using namespace microreactor;


char* QuicStreamId::EncodeMessage(char* first, char* last) const
{
    return first;
}

const char* QuicStreamId::DecodeMessage(const char* first, const char* last)
{
    return first;
}
