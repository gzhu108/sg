#include "QuicFrame.h"

using namespace microreactor;


char* QuicFrame::EncodeMessage(char* first, char* last) const
{
    return first;
}

const char* QuicFrame::DecodeMessage(const char* first, const char* last)
{
    return first;
}
