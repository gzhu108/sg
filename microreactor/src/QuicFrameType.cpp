#include "QuicFrameType.h"

using namespace microreactor;


char* QuicFrameType::EncodeMessage(char* first, char* last) const
{
    return first;
}

const char* QuicFrameType::DecodeMessage(const char* first, const char* last)
{
    return first;
}
